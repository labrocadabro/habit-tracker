import { describe, it, expect } from 'vitest';

import * as utils from "../src/utils.js";

describe('utils', () => {
	describe("buildHabits", () => {
		it('creates habit from localStorage data', () => {
			const storage = `"[{\"_id\":\"habit-test\",\"_streak\":0,\"name\":\"test\",\"_dates\":[]}]"`;
			
		}); 

	}); 
});